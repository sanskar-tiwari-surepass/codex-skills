interface:
  display_name: "Scratchpad"
  short_description: "Default durable repo memory"
  default_prompt: "Use $scratchpad at the start of most repo-backed tasks to read existing scratchpad memory, then update it with any durable lessons before finishing."
policy:
  allow_implicit_invocation: true
